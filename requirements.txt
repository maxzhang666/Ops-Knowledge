# Web
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# Database
sqlalchemy[asyncio]==2.0.40
asyncpg==0.30.0
alembic==1.15.2

# Auth
python-jose[cryptography]==3.4.0
bcrypt>=4.0.0
email-validator==2.2.0

# Validation & Config
pydantic==2.11.3
pydantic-settings==2.9.1

# Redis
redis[hiredis]==5.3.1

# Vector DB
pymilvus==2.6.10

# Object Storage
boto3==1.38.12

# HTTP client
httpx==0.28.1

# Task queue
celery[redis]==5.5.2

# LLM providers
openai>=1.50.0            # primary: OpenAI-compatible Provider adapter
anthropic>=0.40.0         # primary: Anthropic Provider adapter
litellm>=1.82.6           # utility (token_counter, completion_cost) + long-tail Provider fallback (Bedrock, Vertex, Cohere, ...)

# Token counting (used for chunk token estimation; litellm also depends on this)
tiktoken>=0.7.0

# Document parsing: markitdown installed separately in setup.sh (--no-deps to skip magika/onnxruntime)
# markitdown==0.1.5

# Logging
structlog>=25.1.0

# Rate limiting
slowapi==0.1.9

# File type detection
python-magic>=0.4.27

# Workflow engine — LangGraph
# Precise version pin; upgrades done manually (see docs/superpowers/plans/2026-04-21-phase1b-plan29-langgraph-migration.md §Dependencies)
langgraph==1.1.0
langgraph-checkpoint-postgres>=2.0.0,<3.0.0
# langgraph-checkpoint-postgres uses psycopg3 for its PG driver. The [binary]
# extra ships a wheel with a bundled libpq so we don't need libpq-dev on the
# host. (Our business code keeps using asyncpg via SQLAlchemy.)
psycopg[binary]>=3.2.0

# Testing
pytest==8.3.5
pytest-asyncio==0.26.0
